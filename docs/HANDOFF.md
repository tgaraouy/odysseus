# HANDOFF — where we are (read this first)

For any fresh Claude session (this Mac *or* the mini PC). The chat doesn't travel; this file +
the git history do. Last updated **2026-06-15**.

> **To resume the original session on this Mac:** `claude --resume` (pick it) or `claude -c`
> from `~/odysseus/odysseus`. A fresh session: read this file + `git log --oneline -30`.

---

## The two repos
| Repo | Remote | Branch | Local (this Mac) |
|---|---|---|---|
| **Odysseus** (agent platform) | `github.com/tgaraouy/odysseus` (fork; upstream `pewdiepie-archdaemon` is read-only) | `setup/local-docker-browser-mcp` | `~/odysseus/odysseus` |
| **MyOwnHealth** (health bridge, was "ToufHealth") | `github.com/tgaraouy/MyOwnHealth` | `feature/odysseus-mcp-bridge` | `~/projects/health-experiment-studio` |

## The live system (this Mac only)
Docker stack (`odysseus`, `chromadb`, `searxng`, `ntfy`, `sandbox`) + host **Ollama** +
the **MyOwnHealth bridge** (launchd timers, still installed under old `com.toufhealth.*` labels
but running the renamed code fine) + **Tailscale serve** → `https://tawfiks-macbook-pro.tail45f122.ts.net`.
`/health` and `/ledger` are the two health UIs.

---

## What's done (by area — see git log for detail)
- **Security hardening:** mid-loop prompt-injection fencing (F-1), pinned-memory provenance (F-3),
  ledger write-confinement (F-6), env-scrub + `bash`/`python` **sandbox** Tiers A/B/C (F-7), secret
  file perms, Secure cookies, chromadb client/server skew fixed (0.6.3→1.5.9), chromadb/ntfy caps.
  Audit in `docs/security/`.
- **Health UI** (`/health`, generated by `MyOwnHealth: mcp/health_hub.py`): 01 Today, **02 Daily
  journal**, **03 Vision & mindset** (+ trend readout), 04 Protocol, 05 Labs, 06 Record, 07 Care
  prep, **08 Nutrition framework** (holistic, flexible — replaced the rigid weekly plan).
  Docs: `docs/gout-lowcarb-protocol.md`, `docs/nutrition-framework.md`, `docs/mindset-review.md`.
- **Provisioning kit** (for new laptops): **one cross-platform path** — the MyOwnHealth bridge is
  now a **container** (`MyOwnHealth/mcp/Dockerfile` + supercronic timers), added to Odysseus
  `docker-compose.yml` as the profile-gated `bridge` service. So macOS **and** Windows install with
  the same `docker compose up -d` — no launchd/systemd/WSL/Ubuntu. (Windows: run from Git Bash.)
  `docs/RUNBOOK.md` (unified), `docs/PROVISIONING.md`, `docs/user-spec.md` (discovery scaffold),
  `scripts/provision.sh` (writes `COMPOSE_PROFILES=bridge` + `BRIDGE_REPO` + WHOOP into `.env`),
  `scripts/seed_bridge_mcp.py` (registers the bridge MCP server at `http://bridge:8770/mcp`).
  The live Mac keeps its **legacy launchd** host bridge (profile unset → no container clash).
- **Rebrand:** ToufHealth → **MyOwnHealth** across that repo + pushed to its remote (marketing
  "Touf" in `docs/market/` intentionally kept).

## What's open / next (prioritized)
1. **Mini-PC install (Windows).** Follow `docs/RUNBOOK.md` (now a single path). Run from **Git
   Bash**; `docker compose up -d --build` brings up the **containerized bridge** too. **Untested
   piece:** the bridge *container* hasn't been run end-to-end yet (the image builds; the live Mac
   stays on launchd so we didn't `up` it there) — first real boot is on the mini PC. Watch:
   `docker compose logs bridge` (genesis seed, first projection writing `/odyssey-data/health-hub.html`),
   then `seed_bridge_mcp.py` + `tools via http` in the odysseus logs.
2. **Discovery phase** = the actual point. Run the `docs/user-spec.md` intake with the user (he is
   first user **and FDE**): §1 Intent + §2 Outcomes first (they drive everything), §3 Data
   sequenced easy×impact, §4 Context, §5 cross-cutting boundaries → §6 mandate → configures the
   shell. *Optional build:* a specialized "Intake" agent + seed the spec into the instance.
3. **provision.sh** doesn't auto-template/install the launchd plists (RUNBOOK §7 is manual) — fold in.
4. **Odysseus-side** docs still say "ToufHealth bridge" in a few places — optional consistency sweep.
5. **Parked by the user:** PR #4105 (`security/agent-tool-hardening` → upstream `dev`) is closed /
   needs reopen with a real description; the sandbox **uid-1000** fix must land there too. User said
   "don't worry about the PR for now." MFA: user to enable in the UI.

## Hard rules / constraints (do not violate)
- **Reasoning:** Euclidean rigor — define terms, state assumptions, **name the rule for every
  inferential move**; never argue by assertion; lead with the user's own data over population
  priors; say "I don't know" plainly. (Memory: `euclidean-argument-rigor.md`.)
- **Health:** never fabricate the user's data; **allopurinol stays 100mg** (clinician decision);
  all health content is educational/organizational, **not diagnosis**.
- **Secrets:** `mcp/.env`, `.whoop_tokens.json`, `protocol.yaml`, the masaic mp4 are gitignored —
  never print or commit them. WHOOP keys live in `<bridge>/mcp/.env` (chmod 600).
- **Deploys:** confirm before restarting the user's live stack; commit/push only when asked.
